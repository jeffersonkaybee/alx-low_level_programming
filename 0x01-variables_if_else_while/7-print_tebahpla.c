#include <stdio.h>

/**
 * main - Entry point
 * author: kaybee
 * Return: Always 0 (Success)
 */
int main(void)
{
	char sammy;

	for (kaybee = 'z'; kaybee >= 'a'; kaybee--)
		putchar(kaybee);
	putchar('\n');
	return (0);
}
