#include <stdio.h>

/**
 * main - Entry point
 * autor: kaybee
 * Return: Always 0 (Success)
 */
int main(void)
{
	int kaybee;
	char jefferson;

	for (kaybee = 48; kaybee < 58; kaybee++)
		putchar(kaybee)
	for (jefferson = 'a'; jefferson <= 'f'; jefferson++)
		putchar(jefferson);
	putchar('\n');
	return (0);
}
