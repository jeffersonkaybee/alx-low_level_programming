#include <stdio.h>

/**
 * main - Entry point
 * author: kaybee
 * Return: Always 0 (Success)
 */
int main(void)
{
	char kaybee;

	for (kaybee = 'a'; kaybee <= 'z'; kaybee++)
	{
		if (kaybee != 'q' && kaybee != 'e')
			putchar(sammy);
	}
	putchar('\n');
	return (0);
}
